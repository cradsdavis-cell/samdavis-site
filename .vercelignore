tests/
node_modules/
.git/
README.md

# Retired code, engineering docs (design specs, plans, reviews, the UI-audit
# loop, prepaid-booking which names a real client) kept as the paper trail.
# README inside. Never served. docs/<slug>/index.html (the public manual,
# generated from the app repo) is NOT affected: this is one directory.
archive/

# Build/maintenance scripts: never runtime, never public
scripts/

# Local scratch renders
scratchpad/

# Brand kit source — internal (carries gap notes + contact details)
brand/
